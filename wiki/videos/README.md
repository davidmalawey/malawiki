# Videos

One page per ingested transcript. Filename matches the transcript slug minus the date prefix (e.g. transcript `2026-03-14_building-a-forge.md` → page `building-a-forge.md`).

See `CLAUDE.md` at the project root for the page template and frontmatter spec.
